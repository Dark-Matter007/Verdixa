import {useEffect,useState}from"react";import{Link}from"react-router-dom";import{ArrowLeft}from"lucide-react";import api from"../services/api";import UserNavigation from"../components/UserNavigation";
export default function Collections(){const[c,setC]=useState([]),[e,setE]=useState(""),[loading,setLoading]=useState(true);const load=()=>{setLoading(true);api.get("/collections").then(r=>setC(r.data||[])).catch(()=>setE("Unable to load collections.")).finally(()=>setLoading(false));};useEffect(load,[]);return <div className="user-page"><UserNavigation/><main className="user-page-container"><Link className="back-button" to="/dashboard"><ArrowLeft size={18}/>Back to Problems</Link><h1>Collections</h1>{loading?<p className="loading">Loading collections...</p>:e?<div className="dashboard-error"><p>{e}</p><button onClick={load}>Retry</button></div>:!c.length?<div className="empty-state"><h3>No collections published yet</h3><p>Check back soon for curated practice sets.</p></div>:<div className="history-list">{c.map(x=><article className="history-card" key={x.id}><div className="history-main"><h2>{x.title}</h2><p>{x.description}</p><small>{x.items?.length||0} problems</small></div><div>{x.items?.map(i=><Link key={i.id} to={`/problems/${i.problem.id}`}>{i.problem.title}</Link>)}</div></article>)}</div>}</main></div>}
